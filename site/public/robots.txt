# robots.txt for https://akosah285.github.io/engineering-portfolio/
#
# Allow all crawlers everywhere. The /dev/* shakedown URLs are excluded
# from the sitemap (so they won't be discovered organically), but we
# also list them here explicitly for any crawler that ignores sitemaps.

User-agent: *
Allow: /

# The dev gallery and demo-kit pages are build-time verification surfaces,
# not user-facing content. Keep them out of indexes.
Disallow: /engineering-portfolio/dev/

# Sitemap discovery — auto-generated by @astrojs/sitemap on every build.
Sitemap: https://akosah285.github.io/engineering-portfolio/sitemap-index.xml
