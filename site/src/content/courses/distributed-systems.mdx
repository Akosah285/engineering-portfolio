---
title: "Distributed Systems & Fields"
term: "WI20"
oneLineTakeaway: "PDEs and vector fields in continuous media — waves, heat diffusion, and electromagnetic field structure, solved both analytically and on a grid."
concepts:
  - wave-equation
  - heat-equation
  - vector-field
  - biot-savart
techTags:
  - Python
  - NumPy
  - Matplotlib
heroDemoLabel: "Charge Field Plotter"
publishedAt: "2026-05-16"
draft: false
interviewPending: true
---

import ChargeFieldVisualizer from "../../components/demos/distributed-systems/charge-field/ChargeFieldVisualizer";
import BiotSavartVisualizer from "../../components/demos/distributed-systems/biot-savart/BiotSavartVisualizer";
import FaradayVisualizer from "../../components/demos/distributed-systems/faraday/FaradayVisualizer";
import { HeatEquationVisualizer } from "../../components/demos/distributed-systems/heat-equation/HeatEquationVisualizer";
import LaplaceRectVisualizer from "../../components/demos/distributed-systems/laplace-rect/LaplaceRectVisualizer";
import RcRlVisualizer from "../../components/demos/distributed-systems/rc-rl/RcRlVisualizer";
import VectorFieldVisualizer from "../../components/demos/distributed-systems/vector-field/VectorFieldVisualizer";
import Wave2DVisualizer from "../../components/demos/distributed-systems/wave-2d/Wave2DVisualizer";
import WaveEquationVisualizer from "../../components/demos/distributed-systems/wave-equation/WaveEquationVisualizer";
import Explainer from "../../components/Explainer.astro";
import Step from "../../components/Step.astro";
import TryThis from "../../components/TryThis.astro";
import MathExpression from "../../components/demo-kit/MathExpression.astro";

<ChargeFieldVisualizer client:idle />

## Reflection

_Pre-interview preview. Akwasi's first-person reflection on this course is pending —
track at [issue #48](https://github.com/Akosah285/engineering-portfolio/issues/48)._

## 1D wave equation (string)

Plucked-string wave equation with reflective boundary conditions.

<Explainer demoTitle="1D wave equation (string)">
  <Step>
    <p>
      Pluck a guitar string and watch the shape travel, oscillate, return,
      and oscillate again. One simple rule governs every plucked string,
      every taut wire, every acoustic-pipe mode. That rule is the wave
      equation, and it falls out of nothing more exotic than Newton's
      second law applied to a tiny segment of the string.
    </p>
  </Step>
  <Step>
    <p>
      Here is the idea in plain English. How fast a point on the string
      accelerates up or down depends on how <em>curved</em> the string is
      right there. If the string is straight at that point, it doesn't
      accelerate. If it's bent upward (like the bottom of a smile), it gets
      yanked up. If it's bent downward (like the top of a frown), it gets
      pushed down. Curvature creates acceleration. That's it.
    </p>
  </Step>
  <Step>
    <p>
      Where does the curvature-acceleration rule come from? Think of a tiny
      segment of string under tension. Tension pulls on both ends. If the
      string is straight there, the two pulls point in opposite directions
      and cancel — no net force. If the string is curved, both pulls have a
      small vertical component pointing toward the "inside" of the curve.
      Newton's <MathExpression latex="F = ma" /> turns that vertical force
      into vertical acceleration.
    </p>
  </Step>
  <Step>
    <p>
      In symbols, that becomes
      <MathExpression display latex="\frac{\partial^2 u}{\partial t^2} = c^2 \, \frac{\partial^2 u}{\partial x^2}" />
      where <em>u</em>(<em>x</em>,<em>t</em>) is the string's height at
      position <em>x</em> and time <em>t</em>. The left side is "how fast
      the string is accelerating right now." The right side is "how curved
      the string is right now," times a constant <MathExpression latex="c^2" />.
      The constant <em>c</em> is the wave speed — bigger for tighter or
      lighter strings.
    </p>
  </Step>
  <Step>
    <p>
      The boundary matters. A guitar string is clamped at both ends —
      <MathExpression latex="u(0, t) = u(L, t) = 0" /> for all time. That
      is what makes it different from a whip or a free-hanging rope. The
      clamps mean only certain "shapes" can vibrate cleanly — the
      <em>modes</em>. Every other initial shape is just a sum of these
      modes, oscillating at different rates.
    </p>
  </Step>
  <Step>
    <p>
      The allowed shapes are
      <MathExpression latex="\sin(m\pi x / L)" /> for
      <em>m</em> = 1, 2, 3, ... The "Fundamental" preset shows mode 1: a
      single bump from one end to the other. The "Second harmonic" preset
      shows mode 2: a full sine wave with a node — a permanently still
      point — in the middle. (Press a string at the 12th fret of a guitar
      and you have forced mode 2.)
    </p>
  </Step>
  <Step>
    <p>
      The demo doesn't use that closed form to draw the animation, though
      — it solves the equation directly on a grid. Replace the smooth
      string with <em>N</em> points spaced <em>dx</em> apart. For each
      interior point, "value next" = 2·(value now) − (value before) +
      curvature term. The curvature term is just (left neighbour + right
      neighbour − 2·middle), which is the discrete version of
      <MathExpression latex="u_{xx}" />. This is the leapfrog scheme.
    </p>
  </Step>
  <Step>
    <p>
      There is one inequality the numerical scheme must respect:
      <MathExpression latex="c \cdot dt / dx \le 1" />. Physically: in
      one timestep <em>dt</em>, the true wave moves a distance
      <em>c</em>·<em>dt</em>. If that distance is more than one grid
      spacing <em>dx</em>, the wave has "skipped" past data the scheme
      needed, and the simulation blows up. This is called the CFL
      condition, and it is the one trap every PDE solver shares.
    </p>
  </Step>
  <Step>
    <p>Try it yourself.</p>
    <TryThis>
      Load the "High resolution (200 pts)" preset. Notice that
      <em>dt</em> drops to 0.002 (versus 0.005 in the fundamental preset)
      precisely because <em>dx</em> shrank by the same factor.
      Doubling resolution roughly doubles the cost in time too. That's
      why "just use a finer grid" is usually too easy a wish in
      simulation work.
    </TryThis>
  </Step>
  <Step>
    <p>
      The featured problem <em>"Standing-wave frequencies on a fixed-end
      string by separation of variables"</em> just below derives the mode
      shapes from scratch by assuming the solution factors as
      <em>X</em>(<em>x</em>) · <em>T</em>(<em>t</em>). For the
      travelling-wave (rather than standing-wave) picture, the featured
      problem <em>"d'Alembert solution to the 1D wave equation"</em>
      decomposes any initial pulse into a left-mover plus a right-mover.
      And to see what happens when two waves meet on a 2D membrane, jump
      to the 2D wave interference demo immediately below.
    </p>
  </Step>
</Explainer>

<WaveEquationVisualizer client:visible />

## 2D wave interference

Two-source interference pattern with adjustable phase, frequency, and source
separation.

<Wave2DVisualizer client:visible />

## 1D heat equation

Finite-difference solver for heat diffusion along a rod; tune the boundary
conditions and watch the profile relax.

<Explainer demoTitle="1D heat equation">
  <Step>
    <p>
      You pull a metal rod out of a fire and grab the cool end. The heat
      doesn't leap to your hand — it crawls along the rod, smoothing out
      the hot spot a little more every second. Eventually the whole rod
      settles to one warm temperature. The heat equation is the rule that
      governs that crawl, and it turns up wherever something diffuses:
      temperature in a wall, ink in still water, even the price of a stock
      option.
    </p>
  </Step>
  <Step>
    <p>
      Forget formulas for a moment. The whole physics is this: heat flows
      from hot to cold, and the flow is faster when the temperature gap is
      bigger. Two beads sitting at 100° and 0° pull on each other much
      harder than two beads at 100° and 99°. So a sharp temperature spike
      levels out quickly at first and then more slowly as it fades. That
      is all "Fourier's law" really says — no Greek letters required.
    </p>
  </Step>
  <Step>
    <p>
      To put this on a computer, chop the rod into <em>N</em> evenly-spaced
      beads, each holding a temperature <em>u<sub>i</sub></em>. Every
      timestep, each interior bead nudges itself toward the average of its
      two neighbours. If a bead is already sitting at the neighbour
      average, nothing happens — it is already "in line". If it is hotter
      than the average it cools; cooler, it warms. That nudge-toward-the-
      average is the discrete Laplacian, and smoothing — not wave-like
      propagation — is the only thing the heat equation ever does.
    </p>
  </Step>
  <Step>
    <p>
      Make it concrete. Take five beads with temperatures
      (0, 10, 20, 30, 0) and a smoothing strength
      <MathExpression latex="r = 0.25" />. Focus on bead <em>i</em> = 3,
      currently at 30. Its neighbours are 20 and 0, so the discrete second
      derivative is <MathExpression latex="(20 - 2 \cdot 30 + 0) = -40" />
      and the new value is
      <MathExpression latex="30 + 0.25 \cdot (-40) = 20" /> — bead 3
      dropped ten degrees in one step, precisely because it was sticking
      out way above the local average. Do this for every interior bead in
      parallel, leave the pinned endpoints alone, and you have advanced
      one timestep.
    </p>
  </Step>
  <Step>
    <p>
      Written for any interior bead, the update rule is
      <MathExpression
        display
        latex="u_i^{n+1} = u_i^n + r\,(u_{i+1}^n - 2 u_i^n + u_{i-1}^n)"
      />
      where the superscript <em>n</em> labels the timestep and
      <MathExpression latex="r = \alpha\, dt / dx^2" /> is the smoothing
      strength per step. The bracketed term is the discrete second
      derivative — three samples reading off how "bent" the temperature
      profile is at that point. Concave-down hot peak: the bracket is
      negative and the bead cools. Concave-up valley: it warms. Curvature
      drives change here too, exactly as it did for the wave equation, but
      with a first time derivative rather than a second.
    </p>
  </Step>
  <Step>
    <p>
      Shrink <em>dx</em> → 0 and <em>dt</em> → 0 and that update rule
      becomes the continuous heat equation:
      <MathExpression
        display
        latex="\frac{\partial u}{\partial t} = \alpha \, \frac{\partial^2 u}{\partial x^2}"
      />
      The constant <MathExpression latex="\alpha" /> (thermal diffusivity)
      sets how fast smoothing happens — bigger for copper, smaller for
      wood. Sine-shaped modes are eigenfunctions: feed the rod a clean
      <MathExpression latex="\sin(m\pi x / L)" /> bump and it decays as
      <MathExpression latex="\exp(-\alpha (m\pi/L)^2 t)" /> while keeping
      its shape. Higher modes (more wiggles) decay much faster than the
      fundamental — the rod forgets fine detail first and broad bumps
      last.
    </p>
  </Step>
  <Step>
    <p>
      There is one trap. The smoothing strength
      <MathExpression latex="r = \alpha\, dt / dx^2" /> must stay at or
      below <MathExpression latex="1/2" /> for this explicit forward Euler
      scheme — that is the CFL condition for diffusion. Step too big and
      the smoothing overshoots: a bead that ought to drift toward its
      neighbours' average instead leaps past, then leaps back further next
      step, and the simulation explodes into ringing negative
      temperatures inside a few iterations. Halve <em>dx</em> for
      resolution and you must quarter <em>dt</em> — diffusion is
      unforgiving that way.
    </p>
  </Step>
  <Step>
    <p>Try it yourself.</p>
    <TryThis>
      Load the <strong>Sine mode 3</strong> preset, then the
      <strong>Gaussian bump</strong> preset. The mode-3 sine wave keeps
      its shape and just shrinks — it is an eigenfunction, so only its
      amplitude decays. The Gaussian bump instead spreads out and
      flattens, because it is a sum of many sine modes and the wigglier
      ones die first. By a few hundred steps the bump looks almost
      identical to the mode-1 sine; every initial condition relaxes
      toward the lowest mode and then quietly fades away.
    </TryThis>
  </Step>
  <Step>
    <p>
      That contrast with the <em>1D wave equation (string)</em> demo just
      above is the whole point of putting the two side by side. The wave
      equation has a second time derivative — energy sloshes between
      potential and kinetic and is preserved, so a pluck rings forever.
      The heat equation has a first time derivative, so energy leaks out
      monotonically and the rod relaxes to silence. Same Laplacian on the
      right-hand side, very different behaviour on the left. The
      <em>Laplace in a rectangle</em> demo immediately below is what is
      left when even the time derivative is zero — the steady state the
      heat equation generalises toward as <em>t</em> → ∞.
    </p>
  </Step>
</Explainer>

<HeatEquationVisualizer client:visible />

## Laplace in a rectangle

Iterative Laplace solver on a rectangular domain with mixed Dirichlet boundaries.

<LaplaceRectVisualizer client:visible />

## Vector field visualizer

Type any 2D vector field; render its arrows, divergence, and curl.

<VectorFieldVisualizer client:visible />

## RC / RL step response

First-order circuit step response paired with its frequency response.

<RcRlVisualizer client:visible />

## Biot-Savart B-field

Magnetic field from a current loop or coil, visualised as field lines and arrows.

<BiotSavartVisualizer client:visible />

## Faraday's law / induced EMF

Slide a magnet through a coil; live flux and induced EMF traces.

<FaradayVisualizer client:visible />

## Featured problems

import FeaturedProblem from "../../components/FeaturedProblem.astro";

<FeaturedProblem
  kind="Featured Solution"
  title="d'Alembert solution to the 1D wave equation"
  attribution="Adapted from Dartmouth ENGS 23, WI20."
>
  <p>
    The 1D wave equation
    <MathExpression latex="u_{tt} = c^2 u_{xx}" /> on the infinite line
    with initial displacement <MathExpression latex="u(x,0) = \varphi(x)" />
    and initial velocity <MathExpression latex="u_t(x,0) = \psi(x)" />.
    Solve in closed form and read off what kinds of disturbances
    propagate at what speed.
  </p>
  <p>
    Change variables: <MathExpression latex="\xi = x - ct" />,
    <MathExpression latex="\eta = x + ct" />. The PDE becomes
    <MathExpression latex="u_{\xi\eta} = 0" />, which integrates twice
    to give <em>any</em> sum of a right-moving and left-moving wave:
  </p>
  <MathExpression
    latex="u(x,t) = F(x - ct) + G(x + ct)"
    display={true}
  />
  <p>
    Match the two initial conditions:
    <MathExpression latex="F(x) + G(x) = \varphi(x)" /> and
    <MathExpression latex="-c F'(x) + c G'(x) = \psi(x)" />. Solving
    for <MathExpression latex="F" /> and <MathExpression latex="G" />
    yields the d'Alembert formula:
  </p>
  <MathExpression
    latex="u(x,t) = \tfrac{1}{2}\bigl[\varphi(x-ct) + \varphi(x+ct)\bigr] + \tfrac{1}{2c}\int_{x-ct}^{x+ct} \psi(s)\, ds"
    display={true}
  />
  <p>
    The geometry is striking: the value of <MathExpression latex="u" />
    at <MathExpression latex="(x,t)" /> depends only on initial data
    inside the past light cone
    <MathExpression latex="[x-ct,\, x+ct]" /> — finite signal speed
    falls out of the math. An initial displacement splits into two
    half-amplitude copies travelling outward at speed
    <MathExpression latex="\pm c" />. An initial velocity profile
    contributes a smeared, integrated tail — which is why striking a
    drumhead (impulsive velocity) makes a longer-lived disturbance than
    plucking a string (instantaneous displacement). The Charge Field
    Plotter above tells the static side of the same story for
    electrostatics; the wave equation is where time enters and
    everything starts propagating.
  </p>
</FeaturedProblem>

<FeaturedProblem
  kind="Featured Solution"
  title="Magnetic field on the axis of a circular current loop"
  attribution="Adapted from Dartmouth ENGS 23, WI20."
>
  <p>
    A circular loop of radius <MathExpression latex="R" /> carries
    steady current <MathExpression latex="I" />. Find the magnetic
    field <MathExpression latex="B(z)" /> on the axis, a distance
    <MathExpression latex="z" /> from the loop's centre.
  </p>
  <p>
    The Biot–Savart law for a current element
    <MathExpression latex="I\, d\boldsymbol\ell" /> at position
    <MathExpression latex="\mathbf r'" /> contributes
    <MathExpression latex="d\mathbf B = (\mu_0/4\pi)\, I\, d\boldsymbol\ell \times (\mathbf r - \mathbf r')/|\mathbf r - \mathbf r'|^3" />
    at the field point. Each loop element is distance
    <MathExpression latex="\sqrt{R^2 + z^2}" /> from the axis point,
    and the cross product gives a vector of magnitude
    <MathExpression latex="R\, d\ell" /> tilted off-axis by angle
    <MathExpression latex="\theta" /> with
    <MathExpression latex="\sin\theta = R/\sqrt{R^2+z^2}" />.
    Symmetry kills the perpendicular components when integrated
    around the loop, leaving only the axial projection:
  </p>
  <MathExpression
    latex="B_z(z) = \frac{\mu_0 I}{4\pi}\,\oint \frac{R}{(R^2 + z^2)^{3/2}}\, d\ell = \frac{\mu_0 I R^2}{2 (R^2 + z^2)^{3/2}}"
    display={true}
  />
  <p>
    Two limits worth checking. At the centre
    (<MathExpression latex="z=0" />)
    <MathExpression latex="B = \mu_0 I / (2R)" />, the standard
    single-loop result. For
    <MathExpression latex="z \gg R" />, expanding gives
    <MathExpression latex="B \approx \mu_0 I R^2 / (2 z^3)" /> — the
    far field of any planar current loop is a magnetic dipole with
    moment <MathExpression latex="m = I \pi R^2" />, and the
    <MathExpression latex="1/z^3" /> fall-off is the signature of a
    dipole.
  </p>
  <p>
    Stack two such loops separated by a distance equal to their
    radius (Helmholtz coils) and the cubic non-uniformity in the
    midplane cancels — the field is uniform to leading order over a
    surprisingly large central region. The Biot–Savart visualizer
    above shows this directly: drop two loops in coaxially, vary the
    separation, and watch the central field flatten near
    <MathExpression latex="d = R" />.
  </p>
</FeaturedProblem>

<FeaturedProblem
  kind="Featured Solution"
  title="Standing-wave frequencies on a fixed-end string by separation of variables"
  attribution="Adapted from Dartmouth ENGS 23, WI20."
>
  <p>
    A string of length <MathExpression latex="L" /> with both ends
    pinned obeys the 1D wave equation
    <MathExpression latex="u_{tt} = c^2 u_{xx}" /> with boundary
    conditions <MathExpression latex="u(0, t) = u(L, t) = 0" /> for
    all time. Show that the only frequencies that can stand on this
    string are
    <MathExpression latex="f_n = n c / (2 L)" /> for
    <MathExpression latex="n = 1, 2, 3, \ldots" />.
  </p>
  <p>
    Assume a separated solution
    <MathExpression latex="u(x, t) = X(x) T(t)" />. Substituting into
    the PDE and dividing by <MathExpression latex="X(x) T(t)" />
    gives
  </p>
  <MathExpression
    latex="\frac{T''(t)}{c^2\, T(t)} = \frac{X''(x)}{X(x)} = -\lambda"
    display={true}
  />
  <p>
    The left side depends only on <MathExpression latex="t" />, the
    right only on <MathExpression latex="x" />, so both must equal a
    constant — call it <MathExpression latex="-\lambda" /> with the
    sign chosen so non-decaying oscillation is possible. The spatial
    equation <MathExpression latex="X'' + \lambda X = 0" /> with
    <MathExpression latex="X(0) = X(L) = 0" /> is a classic
    Sturm–Liouville problem whose only non-trivial solutions are
    <MathExpression latex="X_n(x) = \sin(n \pi x / L)" /> with
    eigenvalues <MathExpression latex="\lambda_n = (n \pi / L)^2" />.
  </p>
  <p>
    The temporal equation
    <MathExpression latex="T'' + c^2 \lambda_n T = 0" /> oscillates
    at angular frequency
    <MathExpression latex="\omega_n = c \sqrt{\lambda_n} = n \pi c / L" />,
    i.e. linear frequency
    <MathExpression latex="f_n = n c / (2 L)" />. The fundamental is
    <MathExpression latex="f_1 = c / (2 L)" />; every higher mode is
    an integer multiple of it — the harmonic series that gives
    plucked-string instruments their consonant character.
  </p>
  <p>
    Lab 3 used this exact relation in reverse. With
    <MathExpression latex="L" /> measured directly and
    <MathExpression latex="f_1" /> read off the spectrum analyser,
    <MathExpression latex="c = 2 L f_1" /> recovered the wave speed
    on the string to within a few percent of the value predicted from
    string density and tension. The wave-equation visualizer above
    lets you set fixed ends and pluck initial conditions, then watch
    the d'Alembert travelling waves bounce, recombine, and self-
    organise into the predicted standing-wave envelope.
  </p>
</FeaturedProblem>

<FeaturedProblem
  kind="Featured Solution"
  title="Method of images — a point charge above a grounded conducting plane"
  attribution="Adapted from Dartmouth ENGS 23, WI20."
>
  <p>
    A point charge <MathExpression latex="+q" /> sits a distance
    <MathExpression latex="d" /> above an infinite grounded
    conducting plane at <MathExpression latex="z = 0" />. Find the
    electrostatic potential in the upper half-space, the induced
    surface charge on the conductor, and the force pulling the real
    charge towards the plane. Solving Laplace's equation directly with
    the boundary condition
    <MathExpression latex="\Phi(x, y, 0) = 0" /> is painful. Solving it
    by the <em>method of images</em> is two lines.
  </p>
  <p>
    Replace the conductor with a fictitious image charge
    <MathExpression latex="-q" /> at <MathExpression latex="(0, 0, -d)" />,
    in the empty half-space below. The superposition of real and image
    potentials,
  </p>
  <MathExpression
    latex="\Phi(x, y, z) = \frac{q}{4\pi\varepsilon_0}\left[\frac{1}{\sqrt{x^2 + y^2 + (z-d)^2}} - \frac{1}{\sqrt{x^2 + y^2 + (z+d)^2}}\right]"
    display={true}
  />
  <p>
    is identically zero on <MathExpression latex="z = 0" />, satisfies
    Laplace's equation everywhere in <MathExpression latex="z > 0" />
    (the only singularity is the real charge), and matches the
    physical configuration at infinity. By the uniqueness theorem for
    Laplace's equation with Dirichlet boundary data, <em>this is the
    answer</em>. The induced surface charge follows from
    <MathExpression latex="\sigma = -\varepsilon_0 \, \partial \Phi / \partial z|_{z=0} = -q d / [2\pi (r^2 + d^2)^{3/2}]" />,
    which integrates to a total of <MathExpression latex="-q" /> on
    the plane.
  </p>
  <p>
    The force on the real charge is the force exerted by the image
    charge alone (the conductor is just a mathematical fiction):
    <MathExpression latex="F = -q^2 / (16 \pi \varepsilon_0 d^2)" />,
    attractive, falling off as <MathExpression latex="1/d^2" /> as if
    the charge were attracted by a single mirror partner. The wave-
    and field-visualisers on this page run the same uniqueness logic
    in motion — replace a boundary with the right virtual source, and
    the messy PDE collapses into a free-space superposition.
  </p>
</FeaturedProblem>

## What's coming

The author's written reflection lands alongside the v7 interview. The demos
and featured problems above are wired and playable today.
